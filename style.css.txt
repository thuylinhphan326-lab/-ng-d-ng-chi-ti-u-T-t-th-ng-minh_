body {
    margin: 0;
    font-family: 'Segoe UI', sans-serif;
    background: #fff6e8 url('https://i.imgur.com/z4wIu2b.png');
    background-size: cover;
}

/* HEADER TẾT */
.hero {
    background: linear-gradient(135deg, #ff1b1b, #ff8c00);
    padding: 40px 20px;
    color: white;
    text-align: center;
    position: relative;
}

.hero::after {
    content: "🎆 🎇 🎆";
    position: absolute;
    bottom: 10px;
    right: 20px;
    font-size: 26px;
    opacity: 0.6;
}

.sub {
    font-size: 18px;
    margin-top: -10px;
}

/* INPUT */
.input-box input {
    padding: 12px;
    border-radius: 6px;
    border: 2px solid gold;
    background: #fff8e0;
    font-size: 16px;
}
.input-box button {
    background: #ff0000;
    color: white;
    padding: 12px 20px;
    border-radius: 6px;
    border: none;
    cursor: pointer;
    margin-left: 8px;
    font-size: 16px;
    font-weight: bold;
}
.input-box button:hover {
    background: #cc0000;
}

/* CARD */
.plan-container {
    padding: 20px;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
}

.card {
    background: #ffffffcc;
    backdrop-filter: blur(3px);
    border-left: 8px solid #ff0000;
    padding: 20px;
    margin: 15px;
    border-radius: 10px;
    width: 330px;
    box-shadow: 0 4px 10px rgba(0,0,0,0.2);
}

.card h2 {
    margin-top: 0;
    color: #c40000;
    font-weight: 800;
}

.card ul li {
    margin-bottom: 8px;
    color: #5c3b00;
    font-size: 15px;
    font-weight: 500;
}
